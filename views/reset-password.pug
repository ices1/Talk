extends layout.pug

block content
    h3.text-center 重置密码
    div
         form(action="/reset-password/post", method='POST')
            label(for="username").mt-4 用户名:
            input(type="text" name='username').form-control
            label(for="username").mt-4 输入新密码：
            input(type="password" name='password').form-control
            button(class="btn btn-primary btn-lg btn-block mt-5") 确认重置
            